export enum OrderStatus {
  CREATED = 'created',
  ASSUMED = 'assumed',
  STARTED = 'started',
  PAUSED = 'paused',
  STOPPED = 'stopped',
  CANCELED = 'canceled',
  SIGNATURE_PENDING = 'signature-pending',
  SIGNATURED = 'signatured',
  FINISHED = 'finished'
}